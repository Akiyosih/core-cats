# Worklog: Phase A-004 (Coverage Expansion + Testnet Runbook)

- date: 2026-03-05
- phase: A
- repo: core-cats
- branch: phase-a-corezeppelin-direct

## Command Summary
1. Extend `foxar/test/CoreCats.t.sol`:
   - per-address mint cap (4th mint revert)
   - sold-out guard (`MAX_SUPPLY` reached)
   - `setSigner` onlyOwner
   - `setMetadataRenderer` onlyOwner
2. Add Core testnet deployment runbook:
   - `docs/CORE_TESTNET_DEPLOY_RUNBOOK.md`
3. Add runbook link into `README.md` Project Docs section.
4. Run `spark test`.
5. Script simulation check:
   - wrong key format reproduces `Wrong private key length`
   - correct 57-byte key format runs deployment script successfully in local simulation.

## Result
- tests: success (`12 passed, 0 failed`)
- CoreCats-specific suite: `10 passed`
- runbook added for repeatable testnet rehearsal
- deploy script local simulation: success (returns deployed addresses in simulation mode)

## Next Action
1. Execute dry-run and broadcast on Core testnet from runbook.
2. Record deployed addresses + verification links in worklog.
3. Add explorer-side post-deploy checklist results.
