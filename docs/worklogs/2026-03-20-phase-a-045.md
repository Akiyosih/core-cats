# 2026-03-20 Phase A-045

## Summary
- refined the `CCATTEST2` canary mint surface after Phase 1 and Phase 2 so the remaining UI emphasis sits on the real gas-bearing approval rather than the multi-account warning foldout
- reduced duplicate client polling by weakening the same-device bridge cadence and electing a single polling leader when the same mint session is opened in multiple tabs
- restored the live `Mint Status` filter surface on the public collection browser so minted and unminted filtering no longer waits behind a hidden UI gate
- completed the remaining live metadata / transparency checks needed to close the rehearsal matrix

## UI / Polling Adjustments

### Mint Surface Emphasis
- removed the temporary warm-color emphasis from the `If you use multiple CorePass accounts` foldout
- strengthened the existing `QR 1 of 2` / `QR 2 of 2` callout instead
- the callout now carries slightly stronger contrast and edge emphasis without changing the overall page language

### Polling Reduction
- desktop and same-device no longer share the exact same bridge polling cadence
- same-device now polls less aggressively because browser focus / pageshow / visibility returns already refresh the session as soon as the user comes back from CorePass
- when the same mint session is opened in more than one tab, only one visible tab now keeps the active polling lease
- session refreshes are still broadcast across tabs so the non-leader tabs stay in sync without issuing duplicate requests

### Browse Surface Presentation
- reduced the green `Minted` badge size so it no longer covers as much of the cat artwork
- re-enabled the `Mint Status` filter block on the collection page immediately rather than hiding it until after the public snapshot arrives
- while the latest public snapshot is still loading, the filter now stays visible and updates in place as soon as the snapshot resolves

## Phase 3 Evidence

### Live Token Evidence
- checked a live minted token on the `CCATTEST2` contract surface
- contract: `cb56cf49bc281a2180113df64deed42d29034d4e77dd`
- sampled token: `#466`
- observed token metadata:
  - `name`: `CCATTEST2 #466`
  - `description`: `CCATTEST2 pilot for final pre-official CorePass mainnet validation. Non-official release.`
  - `attributesCount`: `5`
  - `imagePrefix`: `data:image/svg+xml;base64,`
  - `tokenUriPrefix`: `data:application/json;base64,`

### Repeatability
- read the same live token twice through the live RPC-backed evidence path
- the `tokenURI` for token `#466` matched on both reads
- this is enough to count the rehearsal metadata repeatability check as complete for the current canary contract

### Renderer Config Surface
- the renderer address remains:
  - `cb58f87513366c8a9b650657494b67e214a1fbfe6977`
- the on-chain data contract remains:
  - `cb7555379589d0776212a22c95de9426e0dffca97daa`
- the live minted token confirms the renderer-backed name prefix and description currently exposed by the deployment:
  - `tokenNamePrefix = CCATTEST2`
  - `tokenDescription = CCATTEST2 pilot for final pre-official CorePass mainnet validation. Non-official release.`

## Phase 4 Closure

### Permissionless Runtime
- no permissionless drift was introduced while closing the Phase 1 / Phase 2 fixes
- the current rehearsal path remains the permissionless `login -> precheck -> commit -> finalize` path rather than the retired signer / allowlist authorization path

### Matrix Closure
- Phase 1 same-seed matrix: complete
  - see `2026-03-20-phase-a-043.md`
- Phase 2 generic-wallet matrix: complete
  - see `2026-03-20-phase-a-044.md`
- transparency surface and explorer links: consistent with the `CCATTEST2` contract / renderer / data addresses
- token metadata and tokenURI repeatability: complete for the sampled live minted token

## Current Conclusion
1. the `CCATTEST2` rehearsal now has both the edge-case wallet evidence and the generic wallet evidence needed to count the mint-flow matrix complete
2. the mint UI now emphasizes the real gas-bearing approval more clearly without over-highlighting the multi-account warning
3. the public collection browser again exposes live minted / unminted filtering as an always-visible control rather than a delayed hidden feature
4. the remaining known UX limitation is still the CorePass-side generic rejection copy for pre-`QR 2 of 2` browser refusals; the authoritative reason remains the browser surface
