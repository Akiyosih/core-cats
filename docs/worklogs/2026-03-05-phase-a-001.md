# Worklog: Phase A-001 (CoreZeppelin Direct)

- date: 2026-03-05
- phase: A
- repo: core-cats
- branch: phase-a-corezeppelin-direct

## Command Summary
1. `spark build` in `core-cats/foxar` (baseline before CoreZeppelin import)
2. `git ls-remote git@github.com:bchainhub/openzeppelin-contracts.git HEAD`
3. `git clone git@github.com:bchainhub/openzeppelin-contracts.git foxar/lib/corezeppelin-contracts`
4. Replace `foxar/src/CoreCats.sol` imports/implementation to CoreZeppelin (`CRC721`, `Ownable`, `EDDSA`) with signature mint + renderer delegate.
5. `spark build` / `spark test`
6. `git submodule add --force git@github.com:bchainhub/openzeppelin-contracts.git foxar/lib/corezeppelin-contracts`

## Result
- baseline result: blocked (`No ylem version exists that matches ^0.8.20`)
- Phase A adaptation result: success (`Ylem 1.1.2 finished`, `Compiler run successful`)
- tests result: success (existing `Counter.t.sol` suite passed)

## Next Action
1. Port `contracts/reference_eth/CoreCatsMetadataRenderer.sol` to `foxar/src` on CoreZeppelin imports and `^1.1.2` pragma.
2. Port `CoreCatsOnchainData.sol` generator output to `foxar/src` and connect via `setMetadataRenderer`.
3. Add dedicated mint/signature/limit/tokenURI tests for `CoreCats` in `foxar/test`.
