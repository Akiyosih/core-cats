# 2026-03-09 Phase A 027 - Prepare the CCATTEST pilot deploy path

Prepared the repository for the optional self-only `CCATTEST` mainnet pilot so the next operator step is the actual deploy window, not more repo-side clarification.

## What changed
1. Added `docs/MAINNET_PILOT_DEPLOY_RUNBOOK.md` with the exact sequence for:
   - pilot labels
   - deployer keystore-based deploy
   - immediate post-deploy checks
   - signer rotation
   - preferred backend/CorePass validation path
   - lower-level script-only validation fallback
2. Updated `foxar/script/CoreCatsCommitMint.s.sol` so the script-only fallback can broadcast in Foxar keystore mode when `DEPLOYER_PRIVATE_KEY` is intentionally absent.
3. Added `foxar/.env.mainnet-pilot.example` for the non-secret pilot label set.
4. Linked the new pilot runbook from:
   - `README.md`
   - `docs/MAINNET_PILOT_FALLBACK.md`
   - `docs/WORK_PROCEDURE_CORE_BLOCKCHAIN.md`

## Result
1. The next concrete operator step can be the actual `CCATTEST` pilot deploy.
2. The repository now distinguishes:
   - fallback decision policy
   - exact pilot operator sequence
3. The runbook keeps the official final canary requirement explicit after pilot success.

## Source references
1. Foxar `spark script` wallet and keystore options:
   - https://foxar.dev/reference/cli/spark/script/
2. Mainnet RPC URL family:
   - https://github.com/core-coin/foxar/blob/master/utils/src/rpc.rs
3. Blockindex mainnet base URL family:
   - https://github.com/core-coin/corebc-rs/blob/master/corebc-core/src/types/network.rs
