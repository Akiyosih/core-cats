# 2026-03-12 Phase A-039

## Summary
- recorded `LT-01` as passed through a real Wallet A quantity-`1` mint on the private canary origin
- confirmed that the Wallet A cumulative path has now reached `2`
- treated the new private-canary host as proven for end-to-end mint traffic on the same run
- kept the separate desktop `QR 2 of 2 -> Step 3` auto-transition issue as a follow-up UX fix, not as a reason to invalidate the on-chain result

## Result
- wallet label: `Wallet A`
- quantity: `1`
- origin: private canary origin
- QR1 path: `device standard camera`
- QR2 path: `continue inside CorePass`
- how far it progressed: `awaiting_identity -> awaiting_commit -> commit_submitted -> commit_confirmed -> finalize_submitted -> finalized`
- any visible error text: none on the successful chain/backend path
- commit tx hash: `0x65bea3800f585b5a267f62b87c3fb33d298f831c13072789a85cdc2e298126df`
- finalize tx hash: `0xd96fa0827200c1653957e76b653127fffcd803083895a4f5daf505af042109ca`
- final result: success
- UX observations:
  - the mint itself completed and the NFT was delivered
  - the private canary origin handled the full `QR1 -> QR2 -> commit -> finalize` path without falling back to the old public canary origin
  - the desktop page still needed follow-up work so that the `QR 2 of 2` stage advances to the post-commit state without a manual reload

## Matrix Consequence
1. `LT-01`: passed
2. Wallet A should now be treated as already at cumulative `2`
3. the next functional Wallet A step is `LT-02`
4. the desktop `QR 2 of 2 -> Step 3` transition remains a UI behavior check, not a blocker against counting the on-chain Wallet A mint itself
