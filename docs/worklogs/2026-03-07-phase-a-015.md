# 2026-03-07 phase-a-015

## Summary
Defined the selected production mint-backend split:

1. Vercel for the public site
2. Contabo Linux for the mint backend
3. SQLite for the first durable store

Also added proxy-capable mint route hooks to `web/` so the existing browser-side flow can later stay on one public origin while the backend moves off Vercel.

## Code / Docs
1. added `docs/MINT_BACKEND_ARCHITECTURE.md`
2. updated `docs/WORK_PROCEDURE_CORE_BLOCKCHAIN.md`
3. updated `docs/WEB_PUBLICATION_POLICY.md`
4. updated `web/README.md`
5. added `web/lib/server/mint-backend-proxy.js`
6. updated mint route handlers to support external backend proxy mode

## Validation
Pending build confirmation after the proxy/config changes.

## Next
1. verify `web` build still succeeds
2. implement the Contabo backend API contract and SQLite-backed session store
