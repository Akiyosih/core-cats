# 2026-03-07 UI 005 - Add web publication policy

## Summary
Added a dedicated web publication policy so teaser publication, hosting, stable origin, and teaser-facing logo-art handling are documented separately from the core launch runbooks.

## What changed
1. Added `docs/WEB_PUBLICATION_POLICY.md`.
2. Linked that policy from:
   - `docs/WORK_PROCEDURE_CORE_BLOCKCHAIN.md`
   - `docs/WEB_UI_MINT_DAPP_SPEC.md`
3. Clarified that the current `web/` should be treated as the long-lived public app rather than as a throwaway teaser-only site.

## Why
The teaser/publication discussion is adjacent to launch-state and web implementation, but not identical to either one. Keeping it in a dedicated policy document makes future updates easier while preserving a clear source of truth.

## Next action
If teaser publication becomes an active task, implement the remaining launch-state work and apply presentation-layer handling for logo-bearing superrare exposure.
