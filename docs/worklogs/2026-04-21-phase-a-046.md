# 2026-04-21 Phase A-046

## Summary
- reviewed the official April 2026 Vercel security bulletin against the live post-mint Core Cats posture
- confirmed the sold-out public host no longer keeps `CORECATS_BACKEND_SHARED_SECRET` in Vercel Production
- confirmed Vercel Preview now contains only `COREPASS_IDENTIFY_METHOD`
- recovered the legacy compatibility host `core-cats-mint.vercel.app` and attached it to the sold-out public deployment
- removed stale preview / historical Vercel aliases so the public alias set now contains only the managed host and the legacy compatibility host
- switched the repo-side canonical mint host from `core-cats-zeta.vercel.app` to `core-cats-mint.vercel.app` while keeping `zeta` as a compatibility alias
- confirmed the Cloudflare browse surface now links to `https://core-cats-mint.vercel.app/mint`
- removed the former `core-cats-zeta.vercel.app` compatibility alias after the browse surface repoint completed

## Source Review
- Vercel bulletin:
  - https://vercel.com/kb/bulletin/vercel-april-2026-security-incident
- Vercel Activity Log docs:
  - https://vercel.com/docs/activity-log
- Vercel Sensitive Environment Variables docs:
  - https://vercel.com/docs/projects/environment-variables/sensitive-environment-variables

## Live Posture Confirmed
1. The public mint is already complete, so the live Vercel surface is now a sold-out public reference rather than an active mint backend.
2. The secret-bearing runtime boundary remains the Contabo backend, not Vercel.
3. The active Contabo backend remains in read-only mode with mint-era write paths retired.
4. The legacy compatibility host now renders the same sold-out experience as the managed Vercel host.

## Incident-Relevant Findings
1. No deployer or finalizer private key is kept in the live Vercel project.
2. The stale production `CORECATS_BACKEND_SHARED_SECRET` was removed because the sold-out public host no longer needs it.
3. Recent Vercel activity reviewed during the incident window matched expected local operator actions.
4. The remaining Vercel env set is public/runtime configuration only.

## Alias Cleanup
1. The live `core-cats.pages.dev` deployment now links to `https://core-cats-mint.vercel.app/mint`.
2. The final named Vercel alias set was reduced to:
   - `https://core-cats-mint.vercel.app`
3. Removed aliases included the former `core-cats-zeta.vercel.app` compatibility alias, the stale private-canary alias, team preview aliases, and legacy `2889s-projects` aliases.

## Current Judgment
1. No additional emergency rotation is required for the live Core Cats post-mint surface.
2. If any future Vercel env must hold a real secret again, store it as a Sensitive Environment Variable and rotate it after any suspected exposure window.
3. Keep the post-mint trust boundary as-is: public/runtime config on Vercel, secrets on Contabo only.
