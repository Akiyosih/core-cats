# 2026-03-08 Phase A-023

## Summary
Documented how `core-cats-eth` should be handled by the time Core mainnet public release happens.

## Changes
1. Added a mainnet-release clarity policy to `docs/IMPLEMENTATION_SOURCE.md`.
2. Updated the main work procedure so the public-launch path now includes a repository-clarity pass.
3. Updated the mainnet closed-launch runbook so the public-launch checklist explicitly demotes `core-cats-eth` to archive-only messaging.
4. Updated `.private/core-cats-mainnet-ops.md` so future local operator work keeps the same archive policy.

## Decision
Default recommendation is:
1. keep `core-cats-eth` as a historical archive
2. do not delete it by default
3. make `core-cats` the only production-facing repository during the real public launch
