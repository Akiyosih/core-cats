# 2026-03-08 Phase A 020 - Reconcile current-state documentation after wallet/funding setup

Audited the private ops notes and repo-side launch docs after the mainnet wallet split, Wallet 6 funding flow, and Foxar keystore-path changes.

## What changed
1. Cleaned private ops notes so the current authoritative state is explicit:
   - Wallet 1 / 2 / 4 are `UTC`
   - Wallet 3 production signer is fresh `RAW`
   - Wallet 2 / 4 / 5 funding from Wallet 6 is recorded
   - historical pre-funding notes are labeled as historical
2. Removed outdated private guidance that still implied:
   - Foxar `probe wallet new` was the current wallet-creation path
   - Wallet 3 should still be treated as a `UTC` wallet in the active plan
   - Wallet 6 funding was still pending
3. Updated the repo-side launch procedure and backend architecture docs so the immediate next steps now reflect the real state:
   - keep Vercel public/closed
   - stage Wallet 3 / Wallet 4 on Contabo
   - keep Wallet 2 deployer handoff for the real deploy window

## Result
1. `.private/core-cats-mainnet-ops.md` is now the authoritative operator snapshot for:
   - wallet roles
   - current funding state
   - Contabo handoff procedure
   - remaining checklist items
2. Repo docs no longer imply that the team is still at an earlier pre-funding/pre-wallet-split stage.

## Source references
1. Foxar `spark script` keystore wallet options:
   - https://foxar.dev/reference/cli/spark/script/
2. Core mainnet RPC family:
   - https://github.com/core-coin/foxar/blob/master/utils/src/rpc.rs
