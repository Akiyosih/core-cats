# 2026-03-08 Phase A 020 - Reconcile public and private current-state documentation

Audited the private ops notes and repo-side launch docs after the role split and keystore-path changes, keeping operator-specific funding/handoff details in private notes.

## What changed
1. Cleaned private ops notes so the current authoritative operator state is explicit, including role assignments, funding, and handoff status.
2. Removed outdated private guidance that still implied:
   - Foxar `probe wallet new` was the current wallet-creation path
   - the signer role should still be treated as a `UTC` wallet in the active plan
   - operator funding/setup was still pending
3. Updated the repo-side launch procedure and backend architecture docs so the immediate next steps reflect the real state without repeating private operational details:
   - keep Vercel public/closed
   - stage signer/finalizer material on the backend host
   - keep deployer material for the real deploy window

## Result
1. `.private/core-cats-mainnet-ops.md` is now the authoritative operator snapshot for:
   - role assignments
   - current funding state
   - backend handoff procedure
   - remaining checklist items
2. Repo docs no longer imply that the team is still at an earlier setup stage, and public docs no longer need to mirror private wallet/funding state.

## Source references
1. Foxar `spark script` keystore wallet options:
   - https://foxar.dev/reference/cli/spark/script/
2. Core mainnet RPC family:
   - https://github.com/core-coin/foxar/blob/master/utils/src/rpc.rs
