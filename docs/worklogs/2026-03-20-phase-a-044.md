# 2026-03-20 Phase A-044

## Summary
- completed the `CCATTEST2` Phase 2 generic-wallet mint matrix against the stable private canary host
- proved that the normal `login`-based mint path works for a non-same-seed rehearsal wallet on both `Desktop QR` and `same-device mobile`
- confirmed that the cumulative `3`-cat wallet cap still rejects the fourth cat before `QR 2 of 2` is prepared
- recorded the result as the clean positive-path counterpart to the Phase 1 same-seed wallet findings

## Matrix Result

### G-01
- target wallet: `Wallet C`
- starting minted count: `0`
- quantity: `2`
- route: `Desktop QR`
- QR1 path: `device standard camera`
- observed result:
  - mint completed successfully
  - the commit transaction and finalize transaction completed normally
  - the success screen, `My Cats`, and explorer surface all reflected the new ownership
- interpretation:
  - the standard generic desktop path works on the current `login`-based flow

### G-02
- target wallet: `Wallet C`
- starting minted count: `2`
- quantity: `1`
- route: `same-device mobile`
- observed result:
  - mint completed successfully
  - the commit transaction and finalize transaction completed normally
  - cumulative owned count reached `3`
- interpretation:
  - the generic same-device mobile path is still valid for a normal single-wallet rehearsal case

### G-03
- target wallet: `Wallet C`
- starting minted count: `3`
- quantity: `1`
- route: `Desktop QR`
- observed result:
  - the browser rejected the request before `QR 2 of 2`
  - no gas-spending commit transaction was prepared
  - the rejection copy matched the cumulative wallet-cap rule
- interpretation:
  - the fourth cat is still blocked at the browser-side precheck stage for the standard mint path

## What This Proves
1. the current `login`-based mint implementation is valid not only for the same-seed edge-case rehearsal from Phase 1, but also for the generic single-wallet path
2. both `Desktop QR` and `same-device mobile` remain usable for a standard non-same-seed wallet
3. the cumulative `3`-cat wallet cap still stops an over-cap request before any gas-spending commit transaction is created
4. Phase 2 can be counted as complete because the clean success path and the clean refusal boundary are both reproduced on the live canary host

## Current Launch Consequence
1. same-seed non-default wallet selection still requires the CorePass in-app QR scanner, as recorded in Phase 1
2. normal single-wallet rehearsal can use either `Desktop QR` or `same-device mobile`
3. the `CCATTEST2` canary now has both the edge-case and the generic-path mint evidence needed for the remaining Phase 3 metadata checks
