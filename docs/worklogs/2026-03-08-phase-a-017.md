# 2026-03-08 Phase A 017 - Harden Contabo mint backend startup for mainnet

Added fail-closed production config validation for the Python mint backend and supplied deployable env / systemd examples for the Contabo target.

## What changed
1. Added `CORECATS_BACKEND_PROFILE` handling in `mint-backend/corecats_mint_backend/config.py`.
2. Added production startup validation so the backend refuses to start if it is still pointing at Devin defaults or missing required secrets / paths.
3. Added `mint-backend/systemd/corecats-mint-backend.env.example` for the intended Contabo env-file pattern.
4. Updated the systemd example to read secrets from `/etc/corecats-mint-backend.env` instead of embedding them directly in the unit file.
5. Updated backend docs and the Contabo runbook to reflect the new production startup guard and mainnet example values.

## Command summary
1. Read backend, proxy, and runbook files.
2. Searched local Foxar / corebc sources for mainnet RPC and Blockindex base URL references.
3. Patched the backend config, systemd example, docs, and env example.

## Result
1. The backend now supports `CORE_RPC_URL` as the preferred mainnet RPC variable.
2. A production-profile backend should no longer silently come up on Devin defaults.
3. The repo now contains a concrete env-file artifact for the Contabo deployment step.

## Source references
1. https://github.com/core-coin/foxar/blob/master/utils/src/rpc.rs
2. https://github.com/core-coin/corebc-rs/blob/master/corebc-core/src/types/network.rs

## Next action
1. Verify the new backend config with local tests.
2. Prepare the Vercel-side production env cutover checklist once the mainnet contract address exists.
