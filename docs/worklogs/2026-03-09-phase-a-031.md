# 2026-03-09 Phase A 031 - De-identify public test fixtures and codify doc boundaries

Reduced unnecessary public operational detail further by replacing a real-world-looking mainnet contract address in backend test fixtures with a clear dummy value and by codifying the public/private documentation boundary for future operator work.

## What changed
1. Replaced the backend test fixture `CORECATS_ADDRESS` value with a dummy `cb...` address that is sufficient for validation logic but does not correspond to the active operator plan.
2. Kept the public repository focused on behavior and trust boundaries rather than operator-specific security posture.
3. Moved the future-documentation boundary rule into local private agent instructions and private ops notes instead of public repo policy text.

## Result
1. Backend production-config tests still validate the intended fail-closed behavior.
2. The repository no longer exposes an unnecessary real-world-looking operator address in current test fixtures.
3. Future local agent work now has an explicit instruction to keep wallet numbering, funding state, handoff timing, host-specific paths, and machine assignment out of the public repository by default.
