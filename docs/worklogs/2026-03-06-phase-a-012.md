# 2026-03-06 Phase A 012 - Probe Core Devin explorer verification

## Summary
Tried to complete Core Devin explorer verification for the current commit-finalize deployment.

## What was attempted
1. Verified current deployed addresses and verify input artifacts.
2. Updated renderer constructor args file to match the latest deployed data contract.
3. Probed public explorer endpoints:
   - `https://xab.blockindex.net/api/`
   - `https://xab.blockindex.net/api/v2/smart-contracts`
4. Attempted `spark verify-contract` against the public explorer with Blockscout verifier mode.

## Result
Automated verification did not complete.
The public endpoints did not behave as a usable Blockscout-compatible verification API.
The immediate error from `spark verify-contract` was:
- `builder error: relative URL without a base`

## Outcome
1. Prepared a manual verification packet:
   - `docs/verify_inputs/devin/VERIFY_SUBMISSION.md`
2. Updated runbook and work procedure to record:
   - automated verify attempt done
   - manual verify likely required

## Next action
Submit the prepared manual verification packet through the explorer's `Get Verified` path or the corresponding Blockindex contact route, unless a proper public verify API is provided.
