# 2026-03-09 Phase A 032 - Fix shell-sourcing of pilot env example

Fixed the pilot env example so it can be sourced directly by `bash` / `Git Bash` during the `CCATTEST` operator session.

## What changed
1. Quoted `CORECATS_TOKEN_DESCRIPTION` in `foxar/.env.mainnet-pilot.example` because the value contains spaces.
2. Updated the matching example in `docs/MAINNET_PILOT_FALLBACK.md` so the documented value matches the shell-safe file.

## Result
1. `source ./.env.mainnet-pilot.example` no longer tries to execute `pilot.exe` as a stray command.
2. The pilot dry-run path on PC2 can continue without inventing a local workaround.
