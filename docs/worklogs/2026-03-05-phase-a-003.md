# Worklog: Phase A-003 (CoreCats Tests + Deploy Script)

- date: 2026-03-05
- phase: A
- repo: core-cats
- branch: phase-a-corezeppelin-direct

## Command Summary
1. Add `foxar/test/CoreCats.t.sol` with CoreCats-specific tests:
   - signature validation
   - nonce/replay rejection
   - expiry rejection
   - tokenURI renderer delegation
2. Add `foxar/script/CoreCatsDeploy.s.sol`:
   - deploy `CoreCatsOnchainData`
   - deploy `CoreCatsMetadataRenderer`
   - deploy `CoreCats`
   - call `setMetadataRenderer`
3. Run:
   - `spark build`
   - `spark test`

## Result
- build: success (`Compiler run successful`)
- tests: success (`8 passed, 0 failed`)
  - `CoreCatsTest` 6/6 passed
  - `CounterTest` 2/2 passed

## Next Action
1. Add deployment runbook for Core testnet (required env vars and exact command line).
2. Add script/test path for signer rotation and operational checks.
3. Start Core testnet deploy rehearsal and explorer verify procedure.
