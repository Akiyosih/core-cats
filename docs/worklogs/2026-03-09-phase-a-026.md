# 2026-03-09 Phase A 026 - Remove public repo legacy scaffolding

Cleaned the public `core-cats` repository so outside reviewers see the active Core launch path more clearly.

## What changed
1. Removed the old root-level `contracts/CoreCats.sol` legacy contract implementation that no longer matched the active Core launch path.
2. Removed obsolete root-level Hardhat-era helper scripts:
   - `scripts/deploy.js`
   - `scripts/mint.js`
   - `scripts/check.js`
3. Removed template `Counter` scaffolding from both the root and nested `foxar/` workspaces.
4. Updated `README.md` and `docs/IMPLEMENTATION_SOURCE.md` so the active workspaces are explicit:
   - `foxar/`
   - `mint-backend/`
   - `web/`

## Result
1. Public readers are less likely to confuse legacy experiments with the current production-facing Core implementation.
2. The repository still keeps intentionally labeled archive/reference imports under:
   - `contracts/reference_eth/`
   - `scripts/reference_eth/`
3. The active launch path remains unchanged; this is a clarity cleanup only.
