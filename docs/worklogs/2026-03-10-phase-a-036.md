# 2026-03-10 Phase A-036

## Summary
- confirmed the public `https://core-cats.vercel.app` canary is still healthy for `mainnet / canary / CCATTEST / relayer=true`
- verified the live per-wallet limit boundary behavior with terminal-side synthetic `identify` callbacks only
- confirmed that over-limit sessions are still rejected before any gas-spending commit transaction is prepared

## Method
1. create a real mint session from the public production origin
2. send only a synthetic `identify` callback for the selected wallet label
3. read back the live session JSON from the same public origin
4. do not submit any commit or finalize transaction

This check validates the live backend/session policy path without spending gas or changing on-chain mint state.

## Results

### Wallet A
- wallet label: `Wallet A`
- quantity: `2`
- QR1 path: terminal-side synthetic `identify` callback only
- QR2 path: not executed
- how far it progressed: session reached `awaiting_commit`
- any visible error text: none
- commit tx hash if any: none
- finalize tx hash if any: none
- final result: allowed exactly up to the cumulative `3`-cat cap; the live session prepared QR 2 of 2
- UX observations: current wallet snapshot returned `minted=1`, `reserved=0`, `availableSlots=2`, `pendingCommitActive=false`

### Wallet A
- wallet label: `Wallet A`
- quantity: `3`
- QR1 path: terminal-side synthetic `identify` callback only
- QR2 path: not prepared
- how far it progressed: session stopped at `authorize_rejected`
- any visible error text: `This wallet is already at the maximum of 3 cats for the standard mint path.`
- commit tx hash if any: none
- finalize tx hash if any: none
- final result: rejected before commit preparation with `wallet_limit_reached`
- UX observations: the live policy boundary matches the intended `1 existing + 3 requested > 3 max` rule

### Wallet B
- wallet label: `Wallet B`
- quantity: `1`
- QR1 path: terminal-side synthetic `identify` callback only
- QR2 path: not prepared
- how far it progressed: session stopped at `authorize_rejected`
- any visible error text: `This wallet is already at the maximum of 3 cats for the standard mint path.`
- commit tx hash if any: none
- finalize tx hash if any: none
- final result: rejected before commit preparation with `wallet_limit_reached`
- UX observations: this confirms the live backend still blocks an already-full wallet before any gas-spending mint transaction is created

## What This Proves
1. the live canary backend still enforces the cumulative `3`-per-wallet rule before QR 2 of 2 is prepared
2. the exact-fill boundary still allows a wallet with `1` existing cat to request quantity `2`
3. an exceed-cap request and an already-full wallet are both refused before commit preparation

## Remaining Gap
1. this was a terminal-side session/API confirmation, not a visual browser confirmation of the blocked-state copy on `/mint`
2. the next live device work should still cover:
   - actual browser confirmation of the blocked-state wording
   - Wallet D manual finalize / recovery / restart behavior
