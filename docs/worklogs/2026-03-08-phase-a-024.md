# 2026-03-08 Phase A 024 - Add Contabo live-state snapshot discipline

Recorded the current Contabo runtime state in the private ops notes and added a rule so future direct server changes leave a current-state breadcrumb.

## What changed
1. Added a timestamped Contabo live-state snapshot to `.private/core-cats-mainnet-ops.md`.
2. Recorded the actual runtime facts discovered by direct SSH inspection:
   - active `gocore`, `caddy`, and `corecats-mint-backend` services
   - temporary public backend origin and restricted Caddy routing
   - active backend still on `mainnet-prep`
   - Wallet 3 / Wallet 4 not yet staged on Contabo
3. Updated `docs/WORK_PROCEDURE_CORE_BLOCKCHAIN.md` so direct Contabo runtime changes now require refreshing the private live-state snapshot.

## Result
1. Repo docs continue to describe the intended release path.
2. Private ops notes now also preserve the current deployed state, reducing operator confusion across threads and sessions.

## Next action
1. Stage Wallet 4 keystore/password on Contabo.
2. Stage Wallet 3 signer secret at the correct handoff stage.
3. Switch the active backend only after the real mainnet contract address exists and preflight passes.
