# 2026-03-10 Phase A-037

## Summary
- completed a live `Wallet D` quantity-1 `CCATTEST` rehearsal run from the public canary `/mint` path
- confirmed a full relayer finalize success after an intentional Contabo backend restart
- captured a clean `OP-02` result: commit confirmed, backend restarted before finalize completed, recovery still succeeded

## Session
- wallet label: `Wallet D`
- quantity: `1`
- public session id: `4d85e83a-7675-4cc2-9c7a-264b96125067`
- public origin: `https://core-cats.vercel.app`

## Result
- QR1 path: desktop-first public `/mint` flow
- QR2 path: desktop-first public `/mint` flow
- how far it progressed: `awaiting_identity -> awaiting_commit -> commit_submitted -> commit_confirmed -> finalize_submitted -> finalized`
- any visible error text: none observed from the live session state
- commit tx hash: `0xc264e219d6569f3a69b713a1f47e27e2b5bdb983a735ba7ba0597cd6d210e047`
- finalize tx hash: `0x0b764b3be4b5b2d679a1ece385801c03076c1baefdc8c980caacdc850d9060e2`
- final result: success

## Recovery / Restart Evidence
1. the session reached `commit_confirmed` on the public canary path
2. the Contabo service was intentionally restarted at `2026-03-10 14:05:58` server local time before finalize completed
3. the backend returned to `active (running)` and resumed serving the same session
4. the session then advanced to `finalize_submitted`
5. the session completed at `finalized`

## Backend Health After Recovery
- `GET http://127.0.0.1:8787/healthz` after the run returned:
  - `networkName=mainnet`
  - `chainId=1`
  - `pendingSessions=0`
  - `stuckSessions=0`
  - `lastError=null`

## Test Matrix Status
- `OP-02`: passed
- `FR-01`: passed through the same live run
- `FR-03`: passed; after QR2 / commit submission and before finalize completed, the operator reloaded or reopened the same `sessionId` URL and the web session continued tracking through finalize completion
