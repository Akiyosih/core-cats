# 2026-03-09 Phase A 029 - Clarify pilot dry-run prerequisites

Clarified the public pilot runbook so the next operator step is the actual `CCATTEST` dry run, without embedding operator-specific workstation details in the public repository.

## What changed
1. Kept the public runbook focused on deploy inputs, build/test, dry run, broadcast deploy, and post-deploy checks.
2. Kept the session setup block that loads pilot labels and deploy inputs before the dry run.
3. Left operator-specific machine-hygiene and secret-handling details to private ops notes instead of public docs.

## Why this matters
1. Public transparency should explain the deploy path and verifiable artifacts, not the operator's personal workstation arrangement.
2. The repository still records the concrete pilot sequence, while private ops notes keep the environment-specific handling details.

## Next action
1. Load the pilot label env.
2. Export the Wallet 2 keystore/password paths and Wallet 2 / Wallet 3 addresses.
3. Run the `CoreCatsDeploy` dry run without `--broadcast`.
4. If the dry run is clean, proceed to broadcast deploy, post-deploy check, and signer rotation.

## Source references
1. Foxar introduction and workspace expectations:
   - https://foxar.dev/intro/
2. Foxar `spark script` wallet and keystore flags:
   - https://foxar.dev/reference/cli/spark/script/
