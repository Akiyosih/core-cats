# 2026-03-09 Phase A 029 - Record PC2 pilot readiness before dry run

Recorded the point where the Windows PC2 pilot operator environment stopped being a setup problem and became ready for the actual `CCATTEST` dry run.

## What changed
1. Updated `docs/MAINNET_PILOT_DEPLOY_RUNBOOK.md` with an explicit PC2 readiness gate:
   - clean clone
   - submodules initialized
   - `spark clean && spark build && spark test`
   - Wallet 2 on keystore/password only
   - Wallet 3 raw absent from PC2
2. Added the recommended PC2 session setup block to the runbook so the next operator step is a real dry run, not more local setup.

## Operational state captured
1. The PC2 environment is now treated as ready for pilot dry run after:
   - repo pull to `7e9a198`
   - clean `git status`
   - successful `spark build`
   - successful `spark test`
2. The remaining work is now operator execution rather than more repository-side repair.

## Next action
1. Load the pilot label env on PC2.
2. Export the Wallet 2 keystore/password paths and Wallet 2 / Wallet 3 addresses.
3. Run the `CoreCatsDeploy` dry run without `--broadcast`.
4. If the dry run is clean, proceed to broadcast deploy, post-deploy check, and signer rotation.

## Source references
1. Foxar introduction and workspace expectations:
   - https://foxar.dev/intro/
2. Foxar `spark script` wallet and keystore flags:
   - https://foxar.dev/reference/cli/spark/script/
