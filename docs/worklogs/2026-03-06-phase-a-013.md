# 2026-03-06 Phase A 013 - Reframe the launch procedure around mainnet closed launch

## Summary
Updated the active work procedure so it no longer treats Phase B / Phase C as the remaining path.

## What changed
1. Rewrote `docs/WORK_PROCEDURE_CORE_BLOCKCHAIN.md` around the actual remaining flow:
   - mainnet readiness
   - web publication before mint opening
   - mainnet closed launch
   - canary validation
   - public launch
2. Added `docs/MAINNET_CLOSED_LAUNCH_RUNBOOK.md`.
3. Updated `docs/WEB_UI_MINT_DAPP_SPEC.md` to add launch-state behavior:
   - `closed`
   - `canary`
   - `public`
4. Updated repository entry-point docs (`README.md`, `docs/IMPLEMENTATION_SOURCE.md`, and ADR-0001 wording) so they no longer imply that B/C remain the active plan.

## Result
The repository now reflects the current reality:
1. direct Core implementation already succeeded
2. remaining work is operational and launch-oriented
3. web publication and mint opening are explicitly separated

## Next action
Implement launch-state handling and durable CorePass session storage in `web/`, then prepare the mainnet deploy/canary checklist against the new runbook.
