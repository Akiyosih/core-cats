export default function SiteFooter() {
  return (
    <footer className="site-footer">
      <p>Core Cats is being prepared as a transparent, free-mint, zero-royalty full on-chain collection.</p>
      <p>Current web layer is built from the repository manifests and renderer-derived viewer data.</p>
    </footer>
  );
}
